CREATE TABLE zoo (
    id INT AUTO_INCREMENT PRIMARY KEY,
    domain VARCHAR(50),
    kingdom VARCHAR(50),
    class VARCHAR(50),
    species VARCHAR(50),
    age INT,
    animalname VARCHAR(100),
    alive BOOLEAN
);
